import SwiftOpenGL
// import Wrap

print("Hello, world!")
// GlfwInit()
// TestPoint()

// var point: UnsafeMutablePointer<Int32> = TestPoint()
// ShowPoint(point)
// var void = WrapPoint()
// Init(void)
// WrapShow(void)

// var haha = CreateValue(100);

// if GLFW_TRUE != glfwInit() {
//     print("Error 1")
// }
// glfwWindowHint(GLFW_VERSION_MAJOR, 3)
// glfwWindowHint(GLFW_VERSION_MINOR, 3)
// glfwWindowHint(GLFW_OPENGL_PROFILE, GLFW_OPENGL_CORE_PROFILE)
// var window = glfwCreateWindow(800, 600, "LearOpenGL", nil, nil)
// if window == nil {
//     print("Error")
// }
// glfwMakeContextCurrent(window)

// glfwInit();
// glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 3);
// glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 3);
// glfwWindowHint(GLFW_OPENGL_PROFILE, GLFW_OPENGL_CORE_PROFILE);

// var window = glfwCreateWindow(800, 600, "LearnOpenGL", nil, nil);
// if window == nil
// {
//     print("Error")
//     glfwTerminate();
// }
// glfwMakeContextCurrent(window)

// func framebuffer_size_callback(window:  UnsafeMutablePointer<GLFWwindow>, width: int, height: int) {
//     glViewport(0, 0, width, height)
// }

// glfwSwtFramebufferSizeCallback(window, framebuffer_size_callback)

// gladLoadGLLoader(GLADloadproc load) 
// GLFWAPI GLFWglproc glfwGetProcAddress(const char* procname);
// print((glfwGetProcAddress as! GLADloadproc))
// gladLoadGLLoader(glfwGetProcAddress as? GLADloadproc)
// gladLoadGLLoader(&glfwGetProcAddress)
// if gladLoadGLLoader(glfwGetProcAddress) != 0
// {
//     print("Failed to initialize GLAD")
// }  